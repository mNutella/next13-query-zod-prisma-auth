export enum API_ROUTES {
  login = "/api/login",
  register = "/api/register",
  post = "/api/post",
  posts = "/api/post/all",
  refresh = "/api/refresh",
}

export enum PAGE_ROUTES {
  main = "/",
  login = "/login",
  profile = "/profile",
  posts = "/posts"
}
